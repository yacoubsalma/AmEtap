<form method="post" action="{{ path('app_suggestion_delete', {'ids': suggestion.ids}) }}" onsubmit="return confirm('Are you sure you want to delete this suggestion?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suggestion.ids) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
