<form method="post" action="{{ path('app_evenement_delete', {'ide': ide}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ide) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
